# CKB Blake2b Solidity

This is a solidity library, which calculates blake2b hash with the same parameters used by CKB.

## usage

Run an ethereum private chain with `geth-genesis.json`, then `truffle test`. 